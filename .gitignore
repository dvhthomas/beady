.build/
.build-app/
build/
.swiftpm/
*.xcodeproj
xcuserdata/
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# The beads backlog stays local: the Dolt database was already ignored, and the
# JSONL export (written by bd's auto-export) shouldn't travel with the code either.
.beads/issues.jsonl
# The interaction log records every field change, old and new values included.
.beads/interactions.jsonl
